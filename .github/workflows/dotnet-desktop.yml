name: Libertix CI compatibility entry point

on:
  workflow_dispatch:

permissions:
  contents: read

# Keep the former workflow name as a manual entry point for existing links.
# The quality, WPF, and ISO policy lives only in ci.yml.
jobs:
  continuous-integration:
    uses: ./.github/workflows/ci.yml
